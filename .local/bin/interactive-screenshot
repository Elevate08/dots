#!/bin/bash
sleep .1
scrot --line 'style=solid,width=2' '%F_%H%M%S.png' -e 'mv $f ~/Pictures/screenshots/' --select
if [ $? -eq 0 ]
then
    notify-send --passivepopup 'Screenshot saved to ~/Pictures/screenshots/'
else
    notify-send --passivepopup 'No Screenshot Taken'
fi
